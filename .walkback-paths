# Auto-learned walkback paths — see SKILL.md.
# Auto-promoted by plugin_update on 2026-06-01
Questionable/Questionable.csproj
Questionable/Questionable.json
# Pinned 2026-06-19: recurring TC-critical fixes reverted by the 3-way merge every refresh.
# - LocalizeShortcut.cs: i18n B-passthrough shim (_L/_LF identity); upstream uses I18N.DotNet
#   (unavailable on TC net9 feed). + no-op Initialize() for the upstream startup call.
# - QuestInfo.cs: CRASH fix — TempQuest (custom 7.5-layout sheet) column-hash-mismatches on game 7.1
#   and crashes plugin LOAD; use stock ExcelQuest. + ItemAction.Action (API15-only) -> AdditionalData
#   GetValueOrDefault<TripleTriadCard>. Carries upstream's TripleTriadCardRewards property too.
# - ItemReward.cs: ItemAction lacks .Action on API12; Mount/Minion/Fashion via .Type, Orchestrion/
#   TripleTriad via AdditionalData typed lookup.
# Repointed 2026-07-29: upstream moved QuestInfo/ItemReward from Questionable.Model to
# Questionable.Domain. The old Model/* pins went stale -- re-applying them produced CS0104
# duplicates against the new Domain types, and dropping them silently took the TempQuest
# load-crash fix with them. Pins now track the new paths.
# See FORWARD_PORT_REFERENCE.md Questionable entry.
Questionable/Utils/LocalizeShortcut.cs
Questionable/Domain/QuestInfo.cs
Questionable/Domain/ItemReward.cs
# Pinned 2026-08-01: this file SHADOWS TC_forward/Directory.Build.props, so it must keep the
# PathMap / SourceLink / GitThisAssembly hygiene block. Without the pin, take-theirs restores
# upstream's analyzer-only version and the plugin silently ships 31 absolute local paths again
# (baked [CallerFilePath] literals) plus a SourceLink blob. See commit 336e4b26.
Directory.Build.props

# 2026-08-30 -- upstream moved the bootstrap to a primary-constructor IAsyncDalamudPlugin
# with LoadAsync/DisposeAsync. IAsyncDalamudPlugin is API15-only, so the TC file is a
# hand-written synchronous IDalamudPlugin: explicit constructor, same-named readonly
# service fields, load performed inline, sync Dispose. Fully TC-owned, hence a pin rather
# than a gate. Cost, accepted deliberately: upstream additions to the service registration
# list no longer arrive on their own and have to be ported in by hand, the same way
# Domain/QuestInfo.cs already works.
Questionable/QuestionablePlugin.cs
